package main

import "fmt"

func main() {
	var fint int
	var fbool bool
	var string string
	string = "Woow this is"
	//Seems like we can have string (or other type names)as variable name
	fmt.Println(string, fint, " and its", fbool)
	//newbool := bool("true")
	//fmt.Println(newbool)
}
